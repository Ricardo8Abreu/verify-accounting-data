{
  "version": "0.1.3",
  "license": "MIT",
  "author": "Ricardo8Abreu",
  "description": "`verify-accounting-data` is an npm package designed to verify the validity of accounting data. It can check if there are elements within an array or characters in a string and return `true` if there are elements or characters and `false` if not.",
  "bugs": {
    "url": "https://github.com/Ricardo8Abreu/verify-accounting-data/issues"
  },
  "repository": {
    "url": "https://github.com/Ricardo8Abreu/verify-accounting-data.git",
    "type": "git"
  },
  "keywords": [
    "javascript",
    "typescript",
    "data",
    "verify",
    "accounting",
    "validation"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "module": "dist/index.mjs",

  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
		"prepublishOnly": "npm run build"
  },
  "peerDependencies": {},
  "prettier": {
    "printWidth": 80,
    "semi": true,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "name": "verify-accounting-data",
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.3.3",
    "jest": "^29.0.0",
    "eslint": "^8.0.0"
  }
}
